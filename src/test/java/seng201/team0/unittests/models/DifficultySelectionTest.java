package seng201.team0.unittests.models;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import seng201.team0.models.DifficultySelection;

import static org.junit.jupiter.api.Assertions.assertEquals;

class DifficultySelectionTest {
    /*
    private DifficultySelection difficultySelectionTest;

    @BeforeEach
    void setupTest() {
        difficultySelectionTest = new DifficultySelection();
    }

    @Test
    void testDifficultySelection() {
        assertEquals("Easy", difficultySelectionTest.getDifficulty());
        difficultySelectionTest.setDifficulty("Medium");
        assertEquals("Medium", difficultySelectionTest.getDifficulty());
    }

     */
}
